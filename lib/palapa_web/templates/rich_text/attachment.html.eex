<figure class="attachment">
  <%= if image?(@embedded_attachment) do %>
    <img src="<%= attachment_url(@embedded_attachment) %>" width="<%= @embedded_attachment.width %>" height="<%= @embedded_attachment.height %>">

    <figcaption class="text-sm">
      <span class="font-bold mr-2"><%= @embedded_attachment.caption %></span> 
      <span><%= link "See full size", to: attachment_url(@embedded_attachment, :original), target: "_blank" %></span>
      <span><%= link "Download", to: attachment_url(@embedded_attachment, :download) %></span>
    </figcaption>
  <% else %>
    <%= link to: @embedded_attachment.url, class: "p-2 border rounded" do %>
      <i class="fas fa-paperclip mr-2"></i> <span class="font-bold"><%= @embedded_attachment.filename %></span> (<%= human_filesize(@embedded_attachment) %>)
    <% end %>
  <% end %>
</figure>
