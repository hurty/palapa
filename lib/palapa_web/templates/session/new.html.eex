<div class="flex justify-center mt-8">
  <div class="flex-3 card-shadow">
    <div class="card p-8">
      <h2 class="card-header">Login</h2>
      
      <div class="card-header-separator"></div>

      <%= form_for @conn, session_path(@conn, :create), [as: :session], fn f -> %>
        <div class="field">
          <%= label f, :email, "Email address", class: "label" %>
          <%= email_input f, :email, class: "input" %>
        </div>

        <div class="field">
          <%= label f, :email, "Password", class: "label" %>
          <%= password_input f, :password, class: "input" %>
        </div>

        <div class="card-actions">
          <%= submit "Log in", class: "btn btn-green" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
