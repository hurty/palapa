<div class="sidebar">
  <div id="toggle-mobile-menu" class="mb-8" data-controller="navigation">
    <button data-action="navigation#toggleMobileMenu" class="flex items-center mx-auto px-3 py-2 border-2 rounded text-green border-green hover:text-green-dark hover:border-green-dark">
      <i class="fas fa-times mr-2"></i>Close menu
    </button>
  </div>

  <%= link(@current_organization.name, to: dashboard_path(@conn, :index, @current_organization), class: "organization-name block") %>
  
  <ul class="header-links">
    <li class="header-link <%= if @navigation == "dashboard" do %>header-link--active<% end %>"> 
      <%= link(to: dashboard_path(@conn, :index, @current_organization), class: "block") do %>
        <i class="fas fa-tachometer-alt mr-2"></i>Dashboard
      <% end %>
    </li>

    <li class="header-link <%= if @navigation == "message" do %>header-link--active<% end %>"> 
      <%= link(to: message_path(@conn, :index, @current_organization), class: "block") do %>
        <i class="fas fa-comment mr-2"></i>Messages
      <% end %>
    </li>

    <li class="header-link  <%= if @navigation == "documents" do %>header-link--active<% end %>">
      <%= link(to: document_path(@conn, :index, @current_organization), class: "block") do %>
        <i class="fas fa-file-alt mr-2"></i>Documents
      <% end %>
    </li>

    <li class="header-link  <%= if @navigation == "members" do %>header-link--active<% end %>">
      <%= link(to: member_path(@conn, :index, @current_organization), class: "block") do %>
        <i class="fas fa-users mr-2"></i>Teams
      <% end %>
    </li>

    <li class="header-link  <%= if @navigation == "settings" do %>header-link--active<% end %>">
      <%= link(to: workspace_path(@conn, :show, @current_organization), class: "block") do %>
        <i class="fas fa-cog mr-2"></i>Settings
      <% end %>
    </li>
  </ul>
</div>
